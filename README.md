# 📦 LinkKeeper

**LinkKeeper** — это self-hosted приложение для хранения и организации ваших закладок. Управляйте ссылками, группируйте их по вкладкам и имейте к ним доступ с любого устройства.

![Docker](https://img.shields.io/badge/docker-%230db7ed.svg?style=for-the-badge&logo=docker&logoColor=white)
![FastAPI](https://img.shields.io/badge/FastAPI-005571?style=for-the-badge&logo=fastapi)
![React](https://img.shields.io/badge/react-%2320232a.svg?style=for-the-badge&logo=react&logoColor=%2361DAFB)
![TailwindCSS](https://img.shields.io/badge/tailwindcss-%2338B2AC.svg?style=for-the-badge&logo=tailwind-css&logoColor=white)

## ✨ Особенности

- 👥 **Многопользовательская система**: Регистрация и авторизация для каждого пользователя.
- 📑 **Вкладки**: Группируйте ссылки по категориям (Работа, Учеба, Развлечения).
- 🔗 **Управление ссылками**: Добавление, редактирование и удаление ссылок в один клик.
- 📱 **Adaptive UI**: Полная поддержка мобильных устройств благодаря Tailwind CSS.
- 🔒 **Безопасность**: Хеширование паролей (bcrypt) и авторизация через JWT-токены.
- 🐳 **Docker Native**: Запуск всей инфраструктуры одной командой.

## 🚀 Быстрый старт

### 1. Подготовка
Убедитесь, что у вас установлены **Docker** и **Docker Compose**.

### 2. Клонирование и настройка
Создайте папку проекта и убедитесь, что структура файлов соответствует проекту. Создайте файл `.env` в корневой директории:

```env
JWT_SECRET=secret_password
```

### 3. Запуск
Выполните команду в терминале:

```bash
docker-compose up -d --build
```

Приложение будет доступно по адресу: **[http://localhost:8080](http://localhost:8080)**

---

## Технологический стек

- **Backend**: FastAPI (Python 3.10) + SQLAlchemy.
- **Frontend**: React 18 + Tailwind CSS.
- **База данных**: SQLite (хранится в локальной папке `./data`).
- **Прокси/Сервер**: Nginx (внутри Docker).

---

## Структура проекта

```text
├── backend/            # Исходный код API (FastAPI)
├── frontend/           # Исходный код клиента (React)
├── data/               # Папка с базой данных (создается автоматически)
├── docker-compose.yml  # Оркестрация контейнеров
└── .env                # Переменные окружения
```

---

### Просмотр логов
Если что-то идет не так, проверьте логи контейнеров:
```bash
docker-compose logs -f backend  # Логи API
docker-compose logs -f frontend # Логи Nginx/UI
```

---

## Лицензия
Проект создан в образовательных целях. Свободен для использования и модификации.
