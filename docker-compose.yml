version: '3.8'

services:
  backend:
    build: ./backend
    environment:
      - JWT_SECRET=your-very-secret-key-here
    volumes:
      - ./data:/data
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: always